# Frontend env. The NEXT_PUBLIC_ prefix is required so the value is inlined
# into the browser bundle at build time.
#
# Local dev:
NEXT_PUBLIC_API_URL=http://localhost:4000
#
# In production point this at your deployed backend URL, e.g.:
# NEXT_PUBLIC_API_URL=https://news-pulse-backend.onrender.com
